pipeline_id: string
trigger:
  type: cron | s3_event
source:
  engine: athena | s3
transform:
  type: sql | dbt
delivery:
  format: csv | parquet | excel
governance:
  pii: boolean
  data_owner: string
  sla_hours: number
